name: Update Discover Section

on:
  schedule:
    # Run weekly on Wednesday at 5 AM UTC
    - cron: '0 5 * * 3'
  workflow_dispatch:
    # Allow manual trigger

jobs:
  update-discover:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: website

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install R packages
        run: |
          Rscript -e 'install.packages(c("httr2", "jsonlite", "dplyr", "stringr", "purrr"))'

      - name: Fetch discover data
        run: |
          Rscript -e 'source("R/fetch_discover.R"); run_discover_fetch()'

      - name: Fetch R Weekly packages
        run: |
          Rscript -e 'source("R/scrape_rweekly.R"); scrape_rweekly()'

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet data/discover.json data/rweekly-packages.json 2>/dev/null; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/discover.json data/rweekly-packages.json
          git commit -m "Update discover section and R Weekly packages

          Daily update of trending, new, rising, and R Weekly featured packages."
          git push
